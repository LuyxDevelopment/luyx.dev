import { NextPage } from 'next';
import InProgress from '../../components/InProgress';

const AdminSettings: NextPage = () => {
	return (
		<InProgress pageName='settings'></InProgress>
	);
};

export default AdminSettings;